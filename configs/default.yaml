# Default config for the baseline

data:
  dataset_name: mnist
  batch_size: 128
  num_workers: 2

model:
  hidden_dim: 64
  input_channels: 1
  output_dims: 1

training:
  epochs: 1
  lr: 0.001
  log_interval: 200
  checkpoint_dir: checkpoints
